# BRIEF 空白模板

复制本文件内容到项目根目录 `BRIEF.md`。未填处保留 `TBD` 哨兵 —— Gate 1 与 Gate 2 都靠 grep 它判断是否可以推进。

标了 `必填` 的节不允许自行编造。其余节可以给默认值，但必须在值后面写 `（默认，未经确认）`。

---

## 0. 元信息

| 项 | 内容 |
|---|---|
| 项目名 | TBD |
| 所属系列 | TBD |
| 版本 | v1 |
| 日期 | TBD |
| 复用模板 | TBD |
| 锁定预设 | TBD（apple-restraint / data-journalism / saas-dark / vertical-feed / 自定义，只填一个） |

---

## 1. 产出目标 `必填`

- 一句话目标：TBD
- 成功的标志是：TBD
- 这不是什么：TBD

> 只写一个目标。写两个以上，两个都会做不好。

---

## 2. 目标受众 `必填`

- 是谁：TBD
- 已知什么：TBD（决定哪些术语可以直接用）
- 不知道什么：TBD
- 观看场景：TBD（手机竖屏通勤 / 桌面横屏专注 / 静音刷流）

> 填「静音刷流」意味着字幕是必需的，且信息不能只靠配音承载。

---

## 3. 规格 `必填`

| 项 | 值 |
|---|---|
| 时长 | TBD 秒 |
| 画幅 | TBD |
| 分辨率 | TBD |
| 帧率 | TBD |
| 格式 | mp4 |
| 质量档 | draft（迭代期）→ high（出片） |
| 分发平台 | TBD |

> 时长写具体数字，不要写「短一点」。选了 `apple-restraint` 要在这里预留 +40%。

---

## 4. 视觉方向 `必填`

**锁定预设后，把该预设的完整参数与 `:root` tokens 粘贴到本节。** 只在这里记录对预设的偏离，每条偏离要写理由。

- 预设：TBD
- 偏离项：TBD（没有就写「无」）
- 风格锚点：TBD（参考链接 / 截图）
- 参考它的什么：TBD（只学配色？只学节奏？说清楚，否则会整体模仿）
- 明确不要学的：TBD

---

## 5. 场景清单 `必填`

| # | 场景名 | 时长 | 画面内容 | 文字/字卡 | 数据来源 | 备注 |
|---|---|---|---|---|---|---|
| 1 | TBD | TBD | TBD | TBD | TBD | TBD |
| 2 | TBD | TBD | TBD | TBD | TBD | TBD |
| 3 | TBD | TBD | TBD | TBD | TBD | TBD |

- 时长合计：TBD（必须等于 §3 的时长）
- 必需场景（缺一不可）：TBD
- 可选场景（时长不够可砍）：TBD

> 这张表的时长列会直接变成 `data-duration`。任何一格空着都没法搭骨架。

---

## 6. 文案与配音

- 旁白全文：TBD（或写「由 agent 依要点撰写，需确认后再制作」）
- 语气：TBD
- 语速：TBD
- 配音方式：TBD（TTS / 自录 / 无旁白）
- TTS 音色 ID：TBD（首字母代表语言，`z` = 普通话）
- 语言版本：TBD

---

## 7. 字幕

- 是否需要：TBD（§2 填了「静音刷流」则此处必须为「是」）
- 样式：TBD（位置 / 字号 / 描边 / 底板）
- 分行规则：TBD（每行最多几字）
- 呈现方式：TBD（整句出现 / 逐词高亮）

---

## 8. 音频

- 背景音乐：TBD；风格：TBD；音量：TBD
- 音效：TBD（哪些节点）
- audio-reactive：TBD
- 素材来源：TBD

---

## 9. 数据源（数据驱动型必填）

- 数据文件路径 / 结构：TBD
- 哪些字段进画面：TBD
- 是否需要批量：TBD；批量数量：TBD
- 单条命名规则：TBD
- 边界情况：TBD（数值为 0 / 缺失 / 超长文本 / 极端值怎么显示）

> 批量场景务必写清边界情况。一百条视频里翻车的永远是那几条异常数据。

---

## 10. 约束与禁止

**锁定预设的禁止清单粘贴到本节。** 再补项目自身的：

- 绝对不能出现：TBD
- 品牌合规要求：TBD
- 时长硬上限：TBD
- 禁止的表达：TBD
- 技术约束：TBD（本仓库默认：不引外部 CDN，GSAP 走项目内 `vendor/`）

---

## 11. 验收标准

通用清单（每条后面必须能贴出证据）：

- [ ] `hyperframes check` 通过 —— 贴输出
- [ ] 时长在 TBD 秒 ± 1 秒内 —— `ffprobe -v error -show_entries format=duration -of csv=p=0 <out>.mp4`
- [ ] 输出 `pix_fmt` 为 `yuv420p` —— `ffprobe -v error -select_streams v:0 -show_entries stream=pix_fmt -of csv=p=0 <out>.mp4`
- [ ] 中文无豆腐块 —— `hyperframes snapshot` 抽查帧
- [ ] `snapshot` 抽查关键时间点：TBD、TBD、TBD
- [ ] 每个场景都有转场，无跳切
- [ ] 内容事实无误（数据与来源逐项核对）

**锁定预设的验收补充粘贴到这里。**

---

# 附：精简版（小改动用）

改个片头、出个图表段，用这个。仍然要落盘成 `BRIEF.md`。

```
目标：______（只写一个）
时长：___秒，画幅 ___，___fps
锁定预设：______
偏离项：______
场景：
  1. ______（__秒）
  2. ______（__秒）
禁止：______
验收：hyperframes check 通过 + snapshot 抽查 __s + 中文正常显示
```
